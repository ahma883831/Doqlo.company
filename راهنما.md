# راهنمای دیپلوی «دوقلو» با Cloudflare Pages

## قدم ۱: ساخت حساب Cloudflare
1. برو **dash.cloudflare.com/sign-up**
2. با ایمیل و رمز عبور ثبت‌نام کن (یا با گیت‌هابت وارد شو، اگه گزینه‌اش بود).
3. ایمیلت رو تأیید کن (لینکی که براش می‌فرسته رو تو ایمیلت باز کن).

## قدم ۲: ساخت پروژه و وصل کردن گیت‌هاب
1. تو داشبورد Cloudflare، از منوی سمت چپ برو **Workers & Pages**.
2. **Create** رو بزن، بعد تب **Pages** رو انتخاب کن.
3. **Connect to Git** رو بزن.
4. اگه اولین‌باره، ازت می‌خواد به گیت‌هابت اجازه بدی — بزن **Authorize** و ریپوی `doqlu` رو انتخاب کن (یا اجازه بده به همه‌ی ریپوهات دسترسی داشته باشه).
5. ریپوی `doqlu` رو از لیست انتخاب کن و **Begin setup** بزن.

## قدم ۳: تنظیمات build (این‌ها رو دقیقاً همینطوری وارد کن)
- **Framework preset:** Vite
- **Build command:** `npm run build`
- **Build output directory:** `dist`

اگه Framework preset گزینه‌ی Vite رو نداشت، دستی همین دو تا فیلد بالا رو پر کن، کافیه.

## قدم ۴: اضافه کردن کلید API (قبل از Deploy)
1. تو همین صفحه، دنبال بخش **Environment variables** بگرد (معمولاً یه دکمه‌ی "Add variable" داره).
2. بزن Add variable:
   - Variable name: `ANTHROPIC_API_KEY`
   - Value: همون کلیدی که از console.anthropic.com گرفتی
3. مطمئن شو تنظیمش برای هر دو حالت **Production** و **Preview** فعاله (معمولاً پیش‌فرض هر دو هست).

## قدم ۵: Deploy
دکمه‌ی **Save and Deploy** رو بزن و چند دقیقه صبر کن. در آخر یه لینک می‌گیری شبیه:
`doqlu.pages.dev`

هر بار که فایل‌های ریپوی گیت‌هابت رو آپدیت کنی، Cloudflare خودش از نو دیپلویش می‌کنه.

## قدم ۶: تست رو گوشی
لینک `doqlu.pages.dev` رو تو مرورگر گوشیت باز کن. اگه گزینه‌ی «افزودن به صفحه اصلی» یا «نصب» اومد، یعنی همه‌چیز درسته.

## قدم ۷: ساخت APK (اختیاری)
1. برو **pwabuilder.com**
2. لینک `doqlu.pages.dev` رو بده و صبر کن تحلیلش کنه.
3. گزینه‌ی **Android** رو بزن و پکیج رو دانلود کن.
4. یه فایل APK/AAB می‌گیری که می‌تونی مستقیم رو گوشی نصب کنی یا رو Google Play بذاری.

---

## اگه فایل‌های جدید رو باید جایگزین کنی
چون کد بک‌اند عوض شده (برای Cloudflare به‌جای Vercel)، تو ریپوی گیت‌هابت:
1. پوشه‌ی `api` (اگه از قبل آپلود کرده بودی) رو حذف کن.
2. یه پوشه‌ی جدید به اسم `functions` بساز و توش یه زیرپوشه‌ی `api` بساز، و فایل `analyze.js` جدید رو توش بذار.
   (یعنی مسیر نهایی باید بشه: `functions/api/analyze.js`)
3. Commit changes بزن.

## نکات
- آیکون‌های فعلی (`public/icons/`) موقتی‌ان، بعداً با لوگوی واقعی جایگزینشون کن.
- کلید API فقط سمت سرور (تو `functions/api/analyze.js`) استفاده می‌شه، پس امنه که کد رو پابلیک رو گیت‌هاب بذاری.
